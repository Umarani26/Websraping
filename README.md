# Simple Websraper
This project demonstrates how to scrape tabular data from a webpage using Python. Specifically, it extracts two columns (Language and Average Annual Salary) from an HTML table and prints the results neatly.
Requirements
Python 3
Requests
BeautifulSoup4
Pandas
